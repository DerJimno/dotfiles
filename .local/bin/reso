#!/usr/bin/env bash
eval $(xrandr | awk '/ connected/{m=$1} /^[ ]+[0-9]+x[0-9]+/ {res=$1;max=0;for(i=2;i<=NF;i++){r=$i;gsub(/[^0-9.]/,"",r);if(r>max)max=r}print "xrandr --output",m,"--mode",res,"--rate",max;exit}')
